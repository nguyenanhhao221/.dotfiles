gui:
  theme:
    selectedLineBgColor:
      - underline
    selectedRangeBgColor:
      - underline
  nerdFontsVersion: "3"
git:
  paging:
    colorArg: always
    pager: delta --dark --paging=never
services:
  'gitlab.dwellerlab.com': 'gitlab:gitlab.dwellerlab.com'
